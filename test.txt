Objective:
I am building a headless RDP configuration on GitHub Actions (Ubuntu 24.04). My goal is to launch an XFCE desktop session via XRDP that is stable, fast, and reliably handles D-Bus/XDG environment variables without crashing the runner.
​The Problem:
I am encountering a persistent "Unable to load a failsafe session" error when connecting. Based on my logs (see 1000045790.jpg), when I attempt to apply a D-Bus initialization fix that involves running sudo systemctl restart dbus or sudo systemctl restart xrdp, the GitHub Actions runner kills the job immediately with Job for xrdp.service canceled and an exit code 1.
​What I have tried:
​D-Bus Inheritance Fix: I implemented a fix to unset the DBUS_SESSION_BUS_ADDRESS and XDG_RUNTIME_DIR variables within the global /etc/xrdp/startwm.sh script to prevent stale environment inheritance.
​Execution Strategy: I am manually creating an RDP user, assigning it sudo privileges, and attempting to launch xfce4-session via a custom ~/.xsession file and a patched startwm.sh.
​The Sabotage: I discovered that restarting the global dbus service inside the GitHub Actions runner sabotages the environment, causing the runner to lose its own internal communication and terminate the process.
​My Requirements:
​I need a robust, non-crashing method to initialize an XFCE session for a custom user that respects the environment without restarting system-level services like dbus.
​Please provide a bash-based configuration for /etc/xrdp/startwm.sh that ensures xfce4-session starts correctly for a non-root user without hitting the "failsafe" error loop.
​Keep in mind this is for a GitHub Actions workflow, so any commands must be non-interactive and avoid systemctl calls that would terminate the runner process.
